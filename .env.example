# ── OpenClaw Gateway Configuration ──
# Port for the agent API (default: 18789)
OPENCLAW_GATEWAY_PORT=18789

# Bind address (lan = accessible on network, 127.0.0.1 = local only)
OPENCLAW_GATEWAY_BIND=lan

# Secret token to secure your agent's API
# Generate one with: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=

# ── Integrations ──
# Add your LLM API keys here (e.g. OPENAI_API_KEY, ANTHROPIC_API_KEY)
TELEGRAM_BOT_TOKEN=

# ── Feature Flags ──
# Optional: Enable/disable features
# ENABLE_WEB_SEARCH=true
